"""An end-to-end demonstration: HLS metadata and its arrays, in one commit.

The stages are deliberately separable so each can be run on its own from a
notebook cell. Nothing here is imported by ``icechest`` itself, so the extra
dependencies stay optional.
"""

from __future__ import annotations
